# Optional: public API origin when the UI is hosted separately (e.g. Vercel).
# Leave unset for local `npm run dev` (Vite proxies to http://127.0.0.1:8000).
# VITE_API_BASE_URL=https://YOUR-CLOUD-RUN-WEB.run.app
